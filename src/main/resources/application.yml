server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/arisa_car
    username: postgres
    password:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true